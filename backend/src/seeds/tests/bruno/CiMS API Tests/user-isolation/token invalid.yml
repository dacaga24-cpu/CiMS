info:
  name: token invalid
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/users/profile"
  headers:
    - name: Authorization
      value: Bearer {{token_invalid}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
