info:
  name: token valid
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/stats"
  headers:
    - name: "Authorization "
      value: Bearer {{token_valid}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
