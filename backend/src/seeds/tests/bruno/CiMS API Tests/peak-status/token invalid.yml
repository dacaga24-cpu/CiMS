info:
  name: token invalid
  type: http
  seq: 1

http:
  method: PUT
  url: "{{base_url}}/api/peak-status/1"
  headers:
    - name: Authorization
      value: Bearer {{token_invalid}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
