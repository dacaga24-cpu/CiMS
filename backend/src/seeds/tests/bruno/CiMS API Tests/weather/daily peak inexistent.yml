info:
  name: daily peak inexistent
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/peaks/99999/weather/daily?days=7"
  headers:
    - name: Authorization
      value: Bearer {{token_valid}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
