info:
  name: hourly missing date
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/api/peaks/1/weather/hourly"
  headers:
    - name: Authorization
      value: Bearer {{token_valid}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
