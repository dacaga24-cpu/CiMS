info:
  name: daily no token
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/peaks/1/weather/daily?days=7"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
