info:
  name: daily token invalid
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/peaks/1/weather/daily?days=7"
  headers:
    - name: Authorization
      value: Bearer {{token_invalid}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
