info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "tu-correo-aqui",
        "password": "tu-contraseña-aqui"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
