info:
  name: profile - token expired
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/auth/profile"
  headers:
    - name: Authorization
      value: Bearer {{token_expired}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
